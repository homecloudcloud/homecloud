import { AST, TmplAstNode } from '@angular/compiler';
import { TemplateExtractorConfig } from './types';
interface ContainerMetaData {
    exp?: AST;
    name: string;
    read?: string;
    spanOffset: {
        start: number;
        end: number;
    };
}
export declare function structuralDirectiveExtractor(config: TemplateExtractorConfig): void;
export declare function traverse(nodes: TmplAstNode[], containers: ContainerMetaData[], config: TemplateExtractorConfig): void;
export {};
