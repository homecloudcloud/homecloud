"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mapDiffToKeys = void 0;
const path_utils_1 = require("../utils/path.utils");
const validators_utils_1 = require("../utils/validators.utils");
function mapDiffToKeys(diffArr, side) {
    const keys = diffArr.reduce((acc, diff) => {
        const base = diff.path.join('.');
        const keys = !(0, validators_utils_1.isObject)(diff[side])
            ? [`'${base}'`]
            : (0, path_utils_1.buildPath)(diff[side]).map((inner) => `'${base}.${inner}'`);
        return acc.push(...keys) && acc;
    }, []);
    return keys.join('\n');
}
exports.mapDiffToKeys = mapDiffToKeys;
//# sourceMappingURL=map-diff-to-keys.js.map