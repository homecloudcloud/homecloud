"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.normalizedGlob = void 0;
const glob_1 = __importDefault(require("glob"));
function normalizedGlob(path, options = {}) {
    // on windows system the path will have `\` which are used a escape characters in glob
    // therefore we have to escape those for the glob to work correctly on those systems
    const normalizedPath = path.replace(/\\/g, '/');
    const mergedOptions = Object.assign(Object.assign({}, options), { ignore: [
            'node_modules/**', 'tmp/**', 'coverage/**', 'dist/**'
        ] });
    return glob_1.default.sync(normalizedPath, mergedOptions);
}
exports.normalizedGlob = normalizedGlob;
//# sourceMappingURL=normalize-glob-path.js.map